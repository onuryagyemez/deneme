{
  "name": "iptv-player",
  "version": "1.0.0",
  "scripts": {
    "build": "mkdir -p public && cp index.html public/ && cp vercel.json public/",
    "vercel-build": "npm run build"
  },
  "dependencies": {}
} 